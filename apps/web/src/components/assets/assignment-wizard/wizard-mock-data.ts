/**
 * @deprecated Presentational MOCK_* defaults removed from wizard steps (CRUD hardening).
 * Kept empty so older test imports fail loudly if reintroduced.
 */

export type WizardSelectOption = { id: string; label: string };
